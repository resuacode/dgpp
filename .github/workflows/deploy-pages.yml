name: Deploy Docusaurus to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Un unico despliegue a la vez. NO se cancela el que este en curso: interrumpir
# un despliegue de GitHub Pages a mitad deja el entorno en un estado inconsistente
# y hace fallar al run siguiente (patron de fallos intermitentes con build verde).
# Se descartan los runs encolados intermedios, pero se deja terminar al que ya
# esta publicando (configuracion recomendada por GitHub para Pages).
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build website
        run: npm run build

      # Genera un PDF por tema (solo contenido visible) dentro de build/pdf/,
      # imprimiendo el sitio ya construido con un navegador headless.
      - name: Generate theme PDFs
        run: npm run generate-pdfs

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          # Reintentos ante fallos transitorios de la API de GitHub Pages.
          timeout: 600000
          error_count: 10
          reporting_interval: 5000
